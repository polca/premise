# Archived metals calculation sources

Files in this directory document calculations behind the runtime configuration.
Premise does not read them while constructing or updating a database.

`conversion_factors_sources.xlsx` is the former conversion-factor workbook. Its
runtime `Conversion factors` sheet was migrated to
`../technology_conversion_factors.yaml`; the remaining sheets are retained as
calculation sources.
