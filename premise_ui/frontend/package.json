{
  "name": "premise-ui-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "benchmark:explorer": "tsc -p tsconfig.benchmark.json && node dist-benchmark/benchmarkExplorer.js",
    "benchmark:explorer:check": "npm run benchmark:explorer -- --scenarios 8 --groups 16 --variables 10 --iterations 10 --assert-baseline"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
